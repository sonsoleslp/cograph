# Legacy Scaling Constants (Pre-v2.0 Behavior)

Scaling constants that preserve the original cograph v1.x behavior. Use
`scaling = "legacy"` to enable these values.

## Usage

``` r
COGRAPH_SCALE_LEGACY
```

## Format

A list with the same structure as `COGRAPH_SCALE`
