# Build cluster_summary from transition vectors

Build cluster_summary from transition vectors

## Usage

``` r
.build_from_transitions(
  from_nodes,
  to_nodes,
  weights,
  cluster_lookup,
  cluster_list,
  method,
  type,
  directed,
  compute_within,
  data = NULL
)
```
