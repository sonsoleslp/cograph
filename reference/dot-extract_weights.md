# Extract Weight Matrix from Network Object

Internal helper to extract adjacency/weight matrix from various input
types.

## Usage

``` r
.extract_weights(x)
```

## Arguments

- x:

  Network object (matrix, CographNetwork, tna, igraph, or list with
  \$weights).

## Value

A numeric matrix.
