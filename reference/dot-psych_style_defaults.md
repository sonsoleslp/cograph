# Psych Network Visual Style Defaults

Returns the standard psych network visual defaults. Used by
`splot(psych_styling = TRUE)` for undirected association networks.

## Usage

``` r
.psych_style_defaults(n_nodes = NULL)
```

## Arguments

- n_nodes:

  Number of nodes (for palette selection).

## Value

A named list of default parameters.
