# Draw Chip Node

Square with pins extending from all edges (processor/IC chip).

## Usage

``` r
draw_chip(
  x,
  y,
  size,
  fill,
  border_color,
  border_width,
  alpha = 1,
  pins_per_side = 3,
  ...
)
```

## Arguments

- pins_per_side:

  Number of pins per side.
