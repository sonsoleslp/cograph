# Draw Neural Node (Base R)

Circle with small connection circles around perimeter.

## Usage

``` r
draw_neural_node_base(
  x,
  y,
  size,
  col,
  border.col,
  border.width,
  n_connections = 6
)
```
