# Check if a package is available

Internal wrapper around requireNamespace that can be mocked in tests.

## Usage

``` r
has_package(pkg)
```

## Arguments

- pkg:

  Package name.

## Value

Logical.
