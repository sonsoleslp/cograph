# Parse SVG Content

Parse SVG from string or file.

## Usage

``` r
parse_svg(svg_data)
```

## Arguments

- svg_data:

  SVG data list from registry.

## Value

Parsed SVG object (grImport2 Picture or NULL).
