# Bootstrap Plotting Methods

Plot methods for bootstrap analysis results from tna::bootstrap(). These
methods visualize bootstrapped TNA models with styling to distinguish
significant from non-significant edges.
