# Nestimate Plotting Methods

Plot methods for Nestimate network objects: `netobject`, `boot_glasso`,
`netobject_group`, and `netobject_ml`. No Nestimate import is needed —
dispatch is via [`inherits()`](https://rdrr.io/r/base/class.html)
class-name checking only.
