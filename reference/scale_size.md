# Create a Size Scale

Map values to sizes.

## Usage

``` r
scale_size(values, range = c(0.03, 0.1), trans = "linear")
```

## Arguments

- values:

  Values to map.

- range:

  Output size range.

- trans:

  Transformation: "linear", "sqrt", "log".

## Value

Scaled values.
